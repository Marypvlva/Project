# Project

Prediction of electrical resistance using video data.

## Project structure

- `src/` — source code
- `data/` — local dataset
- `checkpoints/` — trained model weights
- `requirements.txt` — Python dependencies